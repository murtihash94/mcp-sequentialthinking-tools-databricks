bundle:
  name: mcp-sequentialthinking-databricks

sync:
  include:
    - .build

artifacts:
  default:
    type: whl
    path: .
    build: uv build --wheel

resources:
  apps:
    mcp-sequentialthinking-databricks:
      name: "mcp-sequentialthinking-databricks"
      description: "MCP Sequential Thinking Tools on Databricks Apps"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true
